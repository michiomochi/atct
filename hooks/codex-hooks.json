{
  "hooks": {
	"SessionStart": [
	  {
		"hooks": [
		  {
			"type": "command",
			"command": "if [ -x \"${ATCT_BIN:-}\" ]; then \"$ATCT_BIN\" session-key --hook-input; fi"
		  }
		]
	  }
	],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "if [ ! -x \"${ATCT_BIN:-}\" ]; then printf '%s\\n' '{\"decision\":\"block\",\"reason\":\"ATCT stop-check failed: command unavailable\"}'; exit 0; fi; \"$ATCT_BIN\" stop-check --hook-input"
          }
        ]
      }
    ]
  }
}
